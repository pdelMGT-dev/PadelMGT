TableRow from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.TableRow` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TableRowProps {
  children: React.ReactNode;
}
```
