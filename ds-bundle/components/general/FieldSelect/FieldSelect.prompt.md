FieldSelect from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.FieldSelect` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface FieldSelectProps {
  className?: string;
  id?: string;
  style?: CSSProperties;
  children?: React.ReactNode;
}
```
