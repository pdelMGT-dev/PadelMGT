StatTile from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.StatTile` (bundle loaded from the root `_ds_bundle.js`).

Small stat block used in the player dashboard hero (value + uppercase label). Wraps `.bs-tile` from globals.css.

## Props

```ts
interface StatTileProps {
  value: string | number;
  label: string;
}
```

## Related

`StatTileGrid`
