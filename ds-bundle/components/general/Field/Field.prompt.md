Field from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.Field` (bundle loaded from the root `_ds_bundle.js`).

Label + control wrapper. Wraps the real `.field` class from globals.css.

## Props

```ts
interface FieldProps {
  label: string;
  children: React.ReactNode;
}
```

## Related

`FieldInput`, `FieldSelect`, `FieldTextarea`
