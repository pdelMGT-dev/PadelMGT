Td from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.Td` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TdProps {
  className?: string;
  id?: string;
  style?: CSSProperties;
  children?: React.ReactNode;
}
```
