import * as React from 'react';

/**
 * Avatar — from padelmgt-design-system@1.0.0.
 */
export interface AvatarProps {
  name: string;
  photoUrl?: string;
  size?: number;
}

export declare const Avatar: React.ComponentType<AvatarProps>;
