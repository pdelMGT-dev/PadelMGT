import * as React from 'react';

/**
 * CardImage — from padelmgt-design-system@1.0.0.
 */
export interface CardImageProps {
  src: string;
  alt: string;
  aspectRatio?: string;
}

export declare const CardImage: React.ComponentType<CardImageProps>;
