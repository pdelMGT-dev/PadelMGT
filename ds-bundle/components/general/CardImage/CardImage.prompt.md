CardImage from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.CardImage` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CardImageProps {
  src: string;
  alt: string;
  aspectRatio?: string;
}
```
