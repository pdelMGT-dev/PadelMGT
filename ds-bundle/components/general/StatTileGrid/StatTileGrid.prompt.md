StatTileGrid from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.StatTileGrid` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface StatTileGridProps {
  children: React.ReactNode;
}
```
