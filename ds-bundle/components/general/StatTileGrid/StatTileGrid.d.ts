import * as React from 'react';

/**
 * StatTileGrid — from padelmgt-design-system@1.0.0.
 */
export interface StatTileGridProps {
  children: React.ReactNode;
}

export declare const StatTileGrid: React.ComponentType<StatTileGridProps>;
