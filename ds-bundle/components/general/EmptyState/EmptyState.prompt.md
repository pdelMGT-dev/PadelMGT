EmptyState from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.EmptyState` (bundle loaded from the root `_ds_bundle.js`).

Dashed placeholder box for empty lists, with title/description/CTA. Matches the Mis Ligas empty state.

## Props

```ts
interface EmptyStateProps {
  title: string;
  description: string;
  action?: React.ReactNode;
}
```
