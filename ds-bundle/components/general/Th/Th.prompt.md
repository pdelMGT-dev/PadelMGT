Th from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.Th` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ThProps {
  className?: string;
  id?: string;
  style?: CSSProperties;
  children?: React.ReactNode;
}
```
