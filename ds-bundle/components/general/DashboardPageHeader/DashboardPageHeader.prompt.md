DashboardPageHeader from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.DashboardPageHeader` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DashboardPageHeaderProps {
  eyebrow: string;
  title: string;
  action?: React.ReactNode;
}
```
