import * as React from 'react';

/**
 * Table — from padelmgt-design-system@1.0.0.
 * @replaces table
 */
export interface TableProps {
  children: React.ReactNode;
  minWidth?: number;
}

export declare const Table: React.ComponentType<TableProps>;
