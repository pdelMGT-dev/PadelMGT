Table from padelmgt-design-system. Use via `window.PadelMGTDesignSystem.Table` (bundle loaded from the root `_ds_bundle.js`).

Responsive table shell — scrolls horizontally on narrow screens. Matches the pattern used across dashboard list pages.

## Props

```ts
interface TableProps {
  children: React.ReactNode;
  minWidth?: number;
}
```

## Related

`TableRow`
